<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"

    package="com.yuanxin.hczzpt">

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/LibAppTheme">
        <activity android:name=".platforms.ui.activity.AddYzxzActivity"></activity>
        <activity android:name=".platforms.ui.activity.XzSelectActivity" />
        <activity android:name=".platforms.ui.activity.AddXzActivity" />
        <activity android:name=".platforms.ui.activity.AddXyrActivity" />
        <activity android:name=".home.ui.activity.MainCriminalSuspectDetailsActivity" />
        <activity android:name=".platforms.ui.activity.TeamDetailsActivity" />
        <activity android:name=".home.ui.activity.XyrBjActivity" />
        <activity android:name=".home.LoginActivity"
            android:launchMode="singleTask"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity"
            android:windowSoftInputMode="stateHidden"
            />


    </application>

</manifest>